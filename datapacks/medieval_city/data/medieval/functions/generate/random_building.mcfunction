# Generate a randomized building at the function origin by using the selector
# Usage: stand at the desired location and run /function medieval:generate/random_building
function medieval:generate/select_variant
